import { validate } from "./helpers/validaciones.js";

function Diffing(nodeReal,nodeVirtual){
validate({str:[nodeReal,nodeVirtual]})

// Sincronizar hijos
  const realChildren = [...realNode.childNodes];
  const virtualChildren = [...virtualNode.childNodes];
  const nodeTotal = Math.max(realChildren.length, virtualChildren.length);
  console.log(nodeTotal)
}

//ejemplos de uso

